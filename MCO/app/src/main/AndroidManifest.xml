<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MCO2"
        tools:targetApi="31">

        <!-- LOGIN -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- REGISTER -->
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- HOME / DASHBOARD -->
        <activity android:name=".HomeActivity" android:exported="false" />

        <!-- DIRECTORY LIST -->
        <activity android:name=".DirectoryActivity" android:exported="false" />

        <!-- MAP / GEOLOCATION SEARCH -->
        <activity android:name=".MapSearchActivity" android:exported="false" />

        <!-- FEEDBACK -->
        <activity android:name=".SubmitFeedbackActivity" android:exported="false" />

        <!-- EMAIL -->
        <activity android:name=".EmailActivity" android:exported="false" />

        <!-- (Optional) Old MainActivity kept temporarily -->
        <activity android:name=".MainActivity" android:exported="false" />
    </application>

</manifest>
